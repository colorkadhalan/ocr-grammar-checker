# Frontend

This is a single static HTML page using a Web Component.

## Run
Open `index.html` in your browser.

Optional: serve via a local server:
```bash
cd frontend
python -m http.server 5173
```
Then open:
- http://localhost:5173
